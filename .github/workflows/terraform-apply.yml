name: terraform-apply

on:
  push:
    branches:
      - main
    paths:
      - "infra/**"

jobs:
  apply:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Placeholder
        run: echo "Terraform apply workflow coming next"
